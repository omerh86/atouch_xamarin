<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Login"
             x:Class="LinphoneXamarin.components.Login">
    <StackLayout x:Name="stack_layout" VerticalOptions="FillAndExpand"
                 HorizontalOptions="FillAndExpand" Orientation="Vertical"  Spacing="15">
        <Label Margin="10" Text="Welcome to Aeonix Touch !" FontSize="Large" TextColor="Black" HorizontalOptions="Center" />
        <StackLayout VerticalOptions="Center" x:Name="stack_registrar">
            <Entry Margin="8" x:Name="username" Placeholder="Username" />
            <Entry Margin="8" x:Name="password" Placeholder="Password" IsPassword="true"  />
            <Entry Margin="8" x:Name="domain" Placeholder="Address"  />
            <!--<Button x:Name="register" Text="Register" Clicked="OnRegisterClicked" HorizontalOptions="Center" />-->
            <Button BackgroundColor="#00688f" TextColor="White" BorderRadius="25" x:Name="register2" Text="Login" Clicked="OnRegisterClicked2" HorizontalOptions="Center" />
            <Button  x:Name="Send" Text="send" IsVisible="False" Clicked="OnsendRequest" HorizontalOptions="Center" />
            <Label x:Name="registration_status" HorizontalOptions="Center" />
        </StackLayout>
    </StackLayout>
</ContentPage>